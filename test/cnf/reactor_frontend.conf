accesslogdir    = "log/frontend";

admin:
{
    uri = "/admin";
    user = "admin";
    password = "admin"; # No need to use real password in test config, just use "admin"
};

plugins:
{
	frontend:
	{
		configfile	= "plugins/frontend.conf";
		libfile = "../../frontend.so";
	};
};

engines:
{
	sputnik:
	{
		configfile	= "engines/sputnik/frontend.conf";
	};
};
